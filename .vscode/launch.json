{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Terraform Plan",
            "type": "extensionHost",
            "request": "launch",
            "args": [
                "--extensionDevelopmentPath=${workspaceFolder}"
            ],
            "preLaunchTask": "terraform-plan"
        },
        {
            "name": "Terraform Validate",
            "type": "extensionHost", 
            "request": "launch",
            "args": [
                "--extensionDevelopmentPath=${workspaceFolder}"
            ],
            "preLaunchTask": "terraform-validate"
        }
    ]
}
